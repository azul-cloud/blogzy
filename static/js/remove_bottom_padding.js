// remove the padding from main since we're getting rid of the footer
$( document ).ready( function() {
    $( '#main' ).css('padding-bottom', '0' );
    $( '#wrap' ).css('padding-bottom', '0' );
    $( '#wrap' ).css('height', '100%' );
});